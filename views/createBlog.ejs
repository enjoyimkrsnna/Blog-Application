<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
 
  <link rel="stylesheet" href="/css/header.css">

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

  
  <body>
    <%- include('partials/header') %>
    
  <div class="container mt-4">
    <% if (user) { %>
      <h1><%= isEdit ? 'Edit' : 'Create' %> Blog</h1>
      <form id="blogForm" action="<%= isEdit ? '/blogs/update/' + blog._id : '/blogs/new' %>" method="POST" enctype="multipart/form-data">
        <div class="form-group">
          <label for="title">Title</label>
          <input type="text" class="form-control" id="title" name="title" value="<%= isEdit ? blog.title : '' %>" required>
        </div>
        <div class="form-group">
          <label for="content">Content</label>
          <textarea class="form-control" id="content" name="content" rows="5" required><%= isEdit ? blog.content : '' %></textarea>
        </div>
        <div class="form-group">
          <label for="image">Image</label>
          <% if (isEdit && blog.image) { %>
            <div>
              <img src="<%= blog.image %>" alt="<%= blog.title %>" style="max-width: 200px; max-height: 200px;">
              <p><a href="<%= blog.image %>" target="_blank">View Current Image</a></p>
            </div>
          <% } %>
          <input type="file" class="form-control-file" id="image" name="image">
        </div>
        <button type="submit" class="btn btn-primary"><%= isEdit ? 'Update' : 'Create' %> Blog</button>
      </form>
      
      </form>
    <% } else { %>
      <p class="text-danger">You must be logged in to create or edit a blog. <a href="/auth/login">Login here</a></p>
    <% } %>
  </div>


  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
